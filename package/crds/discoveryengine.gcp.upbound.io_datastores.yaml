---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datastores.discoveryengine.gcp.upbound.io
spec:
  group: discoveryengine.gcp.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - gcp
    kind: DataStore
    listKind: DataStoreList
    plural: datastores
    singular: datastore
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DataStore is the Schema for the DataStores API. Data store is
          a collection of websites and documents used to find answers for end-user's
          questions in Discovery Engine (a.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataStoreSpec defines the desired state of DataStore
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  advancedSiteSearchConfig:
                    description: |-
                      Configuration data for advance site search.
                      Structure is documented below.
                    properties:
                      disableAutomaticRefresh:
                        description: If set true, automatic refresh is disabled for
                          the DataStore.
                        type: boolean
                      disableInitialIndex:
                        description: If set true, initial indexing is disabled for
                          the DataStore.
                        type: boolean
                    type: object
                  contentConfig:
                    description: |-
                      The content config of the data store.
                      Possible values are: NO_CONTENT, CONTENT_REQUIRED, PUBLIC_WEBSITE.
                    type: string
                  createAdvancedSiteSearch:
                    description: |-
                      If true, an advanced data store for site search will be created. If the
                      data store is not configured as site search (GENERIC vertical and
                      PUBLIC_WEBSITE contentConfig), this flag will be ignored.
                    type: boolean
                  displayName:
                    description: |-
                      The display name of the data store. This field must be a UTF-8 encoded
                      string with a length limit of 128 characters.
                    type: string
                  documentProcessingConfig:
                    description: |-
                      Configuration for Document understanding and enrichment.
                      Structure is documented below.
                    properties:
                      chunkingConfig:
                        description: |-
                          Whether chunking mode is enabled.
                          Structure is documented below.
                        properties:
                          layoutBasedChunkingConfig:
                            description: |-
                              Configuration for the layout based chunking.
                              Structure is documented below.
                            properties:
                              chunkSize:
                                description: |-
                                  The token size limit for each chunk.
                                  Supported values: 100-500 (inclusive). Default value: 500.
                                type: number
                              includeAncestorHeadings:
                                description: |-
                                  Whether to include appending different levels of headings to chunks from the middle of the document to prevent context loss.
                                  Default value: False.
                                type: boolean
                            type: object
                        type: object
                      defaultParsingConfig:
                        description: |-
                          Configurations for default Document parser. If not specified, this resource
                          will be configured to use a default DigitalParsingConfig, and the default parsing
                          config will be applied to all file types for Document parsing.
                          Structure is documented below.
                        properties:
                          digitalParsingConfig:
                            description: Configurations applied to digital parser.
                            type: object
                          layoutParsingConfig:
                            description: |-
                              Configurations applied to layout parser.
                              Structure is documented below.
                            properties:
                              enableImageAnnotation:
                                description: If true, the LLM based annotation is
                                  added to the image during parsing.
                                type: boolean
                              enableTableAnnotation:
                                description: If true, the LLM based annotation is
                                  added to the table during parsing.
                                type: boolean
                              excludeHtmlClasses:
                                description: List of HTML classes to exclude from
                                  the parsed content.
                                items:
                                  type: string
                                type: array
                              excludeHtmlElements:
                                description: List of HTML elements to exclude from
                                  the parsed content.
                                items:
                                  type: string
                                type: array
                              excludeHtmlIds:
                                description: List of HTML ids to exclude from the
                                  parsed content.
                                items:
                                  type: string
                                type: array
                              structuredContentTypes:
                                description: 'Contains the required structure types
                                  to extract from the document. Supported values:
                                  shareholder-structure.'
                                items:
                                  type: string
                                type: array
                            type: object
                          ocrParsingConfig:
                            description: |-
                              Configurations applied to OCR parser. Currently it only applies to PDFs.
                              Structure is documented below.
                            properties:
                              useNativeText:
                                description: If true, will use native text instead
                                  of OCR text on pages containing native text.
                                type: boolean
                            type: object
                        type: object
                      parsingConfigOverrides:
                        description: 'Map from file type to override the default parsing
                          configuration based on the file type. Supported keys:'
                        items:
                          properties:
                            digitalParsingConfig:
                              description: Configurations applied to digital parser.
                              type: object
                            fileType:
                              description: The identifier for this object. Format
                                specified above.
                              type: string
                            layoutParsingConfig:
                              description: |-
                                Configurations applied to layout parser.
                                Structure is documented below.
                              properties:
                                enableImageAnnotation:
                                  description: If true, the LLM based annotation is
                                    added to the image during parsing.
                                  type: boolean
                                enableTableAnnotation:
                                  description: If true, the LLM based annotation is
                                    added to the table during parsing.
                                  type: boolean
                                excludeHtmlClasses:
                                  description: List of HTML classes to exclude from
                                    the parsed content.
                                  items:
                                    type: string
                                  type: array
                                excludeHtmlElements:
                                  description: List of HTML elements to exclude from
                                    the parsed content.
                                  items:
                                    type: string
                                  type: array
                                excludeHtmlIds:
                                  description: List of HTML ids to exclude from the
                                    parsed content.
                                  items:
                                    type: string
                                  type: array
                                structuredContentTypes:
                                  description: 'Contains the required structure types
                                    to extract from the document. Supported values:
                                    shareholder-structure.'
                                  items:
                                    type: string
                                  type: array
                              type: object
                            ocrParsingConfig:
                              description: |-
                                Configurations applied to OCR parser. Currently it only applies to PDFs.
                                Structure is documented below.
                              properties:
                                useNativeText:
                                  description: If true, will use native text instead
                                    of OCR text on pages containing native text.
                                  type: boolean
                              type: object
                          type: object
                        type: array
                    type: object
                  industryVertical:
                    description: |-
                      The industry vertical that the data store registers.
                      Possible values are: GENERIC, MEDIA, HEALTHCARE_FHIR.
                    type: string
                  kmsKeyName:
                    description: |-
                      KMS key resource name which will be used to encrypt resources:
                      /{project}/locations/{location}/keyRings/{keyRing}/cryptoKeys/{keyId}
                      The KMS key to be used to protect this DataStore at creation time. Must be
                      set for requests that need to comply with CMEK Org Policy protections.
                      If this field is set and processed successfully, the DataStore will be
                      protected by the KMS key, as indicated in the cmek_config field.
                    type: string
                  location:
                    description: |-
                      The geographic location where the data store should reside. The value can
                      only be one of "global", "us" and "eu".
                    type: string
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  skipDefaultSchemaCreation:
                    description: |-
                      A boolean flag indicating whether to skip the default schema creation for
                      the data store. Only enable this flag if you are certain that the default
                      schema is incompatible with your use case.
                      If set to true, you must manually create a schema for the data store
                      before any documents can be ingested.
                      This flag cannot be specified if data_store.starting_schema is
                      specified.
                    type: boolean
                  solutionTypes:
                    description: |-
                      The solutions that the data store enrolls.
                      Each value may be one of: SOLUTION_TYPE_RECOMMENDATION, SOLUTION_TYPE_SEARCH, SOLUTION_TYPE_CHAT, SOLUTION_TYPE_GENERATIVE_CHAT.
                    items:
                      type: string
                    type: array
                required:
                - location
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  advancedSiteSearchConfig:
                    description: |-
                      Configuration data for advance site search.
                      Structure is documented below.
                    properties:
                      disableAutomaticRefresh:
                        description: If set true, automatic refresh is disabled for
                          the DataStore.
                        type: boolean
                      disableInitialIndex:
                        description: If set true, initial indexing is disabled for
                          the DataStore.
                        type: boolean
                    type: object
                  contentConfig:
                    description: |-
                      The content config of the data store.
                      Possible values are: NO_CONTENT, CONTENT_REQUIRED, PUBLIC_WEBSITE.
                    type: string
                  createAdvancedSiteSearch:
                    description: |-
                      If true, an advanced data store for site search will be created. If the
                      data store is not configured as site search (GENERIC vertical and
                      PUBLIC_WEBSITE contentConfig), this flag will be ignored.
                    type: boolean
                  displayName:
                    description: |-
                      The display name of the data store. This field must be a UTF-8 encoded
                      string with a length limit of 128 characters.
                    type: string
                  documentProcessingConfig:
                    description: |-
                      Configuration for Document understanding and enrichment.
                      Structure is documented below.
                    properties:
                      chunkingConfig:
                        description: |-
                          Whether chunking mode is enabled.
                          Structure is documented below.
                        properties:
                          layoutBasedChunkingConfig:
                            description: |-
                              Configuration for the layout based chunking.
                              Structure is documented below.
                            properties:
                              chunkSize:
                                description: |-
                                  The token size limit for each chunk.
                                  Supported values: 100-500 (inclusive). Default value: 500.
                                type: number
                              includeAncestorHeadings:
                                description: |-
                                  Whether to include appending different levels of headings to chunks from the middle of the document to prevent context loss.
                                  Default value: False.
                                type: boolean
                            type: object
                        type: object
                      defaultParsingConfig:
                        description: |-
                          Configurations for default Document parser. If not specified, this resource
                          will be configured to use a default DigitalParsingConfig, and the default parsing
                          config will be applied to all file types for Document parsing.
                          Structure is documented below.
                        properties:
                          digitalParsingConfig:
                            description: Configurations applied to digital parser.
                            type: object
                          layoutParsingConfig:
                            description: |-
                              Configurations applied to layout parser.
                              Structure is documented below.
                            properties:
                              enableImageAnnotation:
                                description: If true, the LLM based annotation is
                                  added to the image during parsing.
                                type: boolean
                              enableTableAnnotation:
                                description: If true, the LLM based annotation is
                                  added to the table during parsing.
                                type: boolean
                              excludeHtmlClasses:
                                description: List of HTML classes to exclude from
                                  the parsed content.
                                items:
                                  type: string
                                type: array
                              excludeHtmlElements:
                                description: List of HTML elements to exclude from
                                  the parsed content.
                                items:
                                  type: string
                                type: array
                              excludeHtmlIds:
                                description: List of HTML ids to exclude from the
                                  parsed content.
                                items:
                                  type: string
                                type: array
                              structuredContentTypes:
                                description: 'Contains the required structure types
                                  to extract from the document. Supported values:
                                  shareholder-structure.'
                                items:
                                  type: string
                                type: array
                            type: object
                          ocrParsingConfig:
                            description: |-
                              Configurations applied to OCR parser. Currently it only applies to PDFs.
                              Structure is documented below.
                            properties:
                              useNativeText:
                                description: If true, will use native text instead
                                  of OCR text on pages containing native text.
                                type: boolean
                            type: object
                        type: object
                      parsingConfigOverrides:
                        description: 'Map from file type to override the default parsing
                          configuration based on the file type. Supported keys:'
                        items:
                          properties:
                            digitalParsingConfig:
                              description: Configurations applied to digital parser.
                              type: object
                            fileType:
                              description: The identifier for this object. Format
                                specified above.
                              type: string
                            layoutParsingConfig:
                              description: |-
                                Configurations applied to layout parser.
                                Structure is documented below.
                              properties:
                                enableImageAnnotation:
                                  description: If true, the LLM based annotation is
                                    added to the image during parsing.
                                  type: boolean
                                enableTableAnnotation:
                                  description: If true, the LLM based annotation is
                                    added to the table during parsing.
                                  type: boolean
                                excludeHtmlClasses:
                                  description: List of HTML classes to exclude from
                                    the parsed content.
                                  items:
                                    type: string
                                  type: array
                                excludeHtmlElements:
                                  description: List of HTML elements to exclude from
                                    the parsed content.
                                  items:
                                    type: string
                                  type: array
                                excludeHtmlIds:
                                  description: List of HTML ids to exclude from the
                                    parsed content.
                                  items:
                                    type: string
                                  type: array
                                structuredContentTypes:
                                  description: 'Contains the required structure types
                                    to extract from the document. Supported values:
                                    shareholder-structure.'
                                  items:
                                    type: string
                                  type: array
                              type: object
                            ocrParsingConfig:
                              description: |-
                                Configurations applied to OCR parser. Currently it only applies to PDFs.
                                Structure is documented below.
                              properties:
                                useNativeText:
                                  description: If true, will use native text instead
                                    of OCR text on pages containing native text.
                                  type: boolean
                              type: object
                          type: object
                        type: array
                    type: object
                  industryVertical:
                    description: |-
                      The industry vertical that the data store registers.
                      Possible values are: GENERIC, MEDIA, HEALTHCARE_FHIR.
                    type: string
                  kmsKeyName:
                    description: |-
                      KMS key resource name which will be used to encrypt resources:
                      /{project}/locations/{location}/keyRings/{keyRing}/cryptoKeys/{keyId}
                      The KMS key to be used to protect this DataStore at creation time. Must be
                      set for requests that need to comply with CMEK Org Policy protections.
                      If this field is set and processed successfully, the DataStore will be
                      protected by the KMS key, as indicated in the cmek_config field.
                    type: string
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  skipDefaultSchemaCreation:
                    description: |-
                      A boolean flag indicating whether to skip the default schema creation for
                      the data store. Only enable this flag if you are certain that the default
                      schema is incompatible with your use case.
                      If set to true, you must manually create a schema for the data store
                      before any documents can be ingested.
                      This flag cannot be specified if data_store.starting_schema is
                      specified.
                    type: boolean
                  solutionTypes:
                    description: |-
                      The solutions that the data store enrolls.
                      Each value may be one of: SOLUTION_TYPE_RECOMMENDATION, SOLUTION_TYPE_SEARCH, SOLUTION_TYPE_CHAT, SOLUTION_TYPE_GENERATIVE_CHAT.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.contentConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.contentConfig)
                || (has(self.initProvider) && has(self.initProvider.contentConfig))'
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
            - message: spec.forProvider.industryVertical is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.industryVertical)
                || (has(self.initProvider) && has(self.initProvider.industryVertical))'
          status:
            description: DataStoreStatus defines the observed state of DataStore.
            properties:
              atProvider:
                properties:
                  advancedSiteSearchConfig:
                    description: |-
                      Configuration data for advance site search.
                      Structure is documented below.
                    properties:
                      disableAutomaticRefresh:
                        description: If set true, automatic refresh is disabled for
                          the DataStore.
                        type: boolean
                      disableInitialIndex:
                        description: If set true, initial indexing is disabled for
                          the DataStore.
                        type: boolean
                    type: object
                  contentConfig:
                    description: |-
                      The content config of the data store.
                      Possible values are: NO_CONTENT, CONTENT_REQUIRED, PUBLIC_WEBSITE.
                    type: string
                  createAdvancedSiteSearch:
                    description: |-
                      If true, an advanced data store for site search will be created. If the
                      data store is not configured as site search (GENERIC vertical and
                      PUBLIC_WEBSITE contentConfig), this flag will be ignored.
                    type: boolean
                  createTime:
                    description: Timestamp when the DataStore was created.
                    type: string
                  defaultSchemaId:
                    description: The id of the default Schema associated with this
                      data store.
                    type: string
                  displayName:
                    description: |-
                      The display name of the data store. This field must be a UTF-8 encoded
                      string with a length limit of 128 characters.
                    type: string
                  documentProcessingConfig:
                    description: |-
                      Configuration for Document understanding and enrichment.
                      Structure is documented below.
                    properties:
                      chunkingConfig:
                        description: |-
                          Whether chunking mode is enabled.
                          Structure is documented below.
                        properties:
                          layoutBasedChunkingConfig:
                            description: |-
                              Configuration for the layout based chunking.
                              Structure is documented below.
                            properties:
                              chunkSize:
                                description: |-
                                  The token size limit for each chunk.
                                  Supported values: 100-500 (inclusive). Default value: 500.
                                type: number
                              includeAncestorHeadings:
                                description: |-
                                  Whether to include appending different levels of headings to chunks from the middle of the document to prevent context loss.
                                  Default value: False.
                                type: boolean
                            type: object
                        type: object
                      defaultParsingConfig:
                        description: |-
                          Configurations for default Document parser. If not specified, this resource
                          will be configured to use a default DigitalParsingConfig, and the default parsing
                          config will be applied to all file types for Document parsing.
                          Structure is documented below.
                        properties:
                          digitalParsingConfig:
                            description: Configurations applied to digital parser.
                            type: object
                          layoutParsingConfig:
                            description: |-
                              Configurations applied to layout parser.
                              Structure is documented below.
                            properties:
                              enableImageAnnotation:
                                description: If true, the LLM based annotation is
                                  added to the image during parsing.
                                type: boolean
                              enableTableAnnotation:
                                description: If true, the LLM based annotation is
                                  added to the table during parsing.
                                type: boolean
                              excludeHtmlClasses:
                                description: List of HTML classes to exclude from
                                  the parsed content.
                                items:
                                  type: string
                                type: array
                              excludeHtmlElements:
                                description: List of HTML elements to exclude from
                                  the parsed content.
                                items:
                                  type: string
                                type: array
                              excludeHtmlIds:
                                description: List of HTML ids to exclude from the
                                  parsed content.
                                items:
                                  type: string
                                type: array
                              structuredContentTypes:
                                description: 'Contains the required structure types
                                  to extract from the document. Supported values:
                                  shareholder-structure.'
                                items:
                                  type: string
                                type: array
                            type: object
                          ocrParsingConfig:
                            description: |-
                              Configurations applied to OCR parser. Currently it only applies to PDFs.
                              Structure is documented below.
                            properties:
                              useNativeText:
                                description: If true, will use native text instead
                                  of OCR text on pages containing native text.
                                type: boolean
                            type: object
                        type: object
                      name:
                        description: |-
                          (Output)
                          The full resource name of the Document Processing Config. Format:
                          projects/{project}/locations/{location}/collections/{collection_id}/dataStores/{data_store_id}/documentProcessingConfig.
                        type: string
                      parsingConfigOverrides:
                        description: 'Map from file type to override the default parsing
                          configuration based on the file type. Supported keys:'
                        items:
                          properties:
                            digitalParsingConfig:
                              description: Configurations applied to digital parser.
                              type: object
                            fileType:
                              description: The identifier for this object. Format
                                specified above.
                              type: string
                            layoutParsingConfig:
                              description: |-
                                Configurations applied to layout parser.
                                Structure is documented below.
                              properties:
                                enableImageAnnotation:
                                  description: If true, the LLM based annotation is
                                    added to the image during parsing.
                                  type: boolean
                                enableTableAnnotation:
                                  description: If true, the LLM based annotation is
                                    added to the table during parsing.
                                  type: boolean
                                excludeHtmlClasses:
                                  description: List of HTML classes to exclude from
                                    the parsed content.
                                  items:
                                    type: string
                                  type: array
                                excludeHtmlElements:
                                  description: List of HTML elements to exclude from
                                    the parsed content.
                                  items:
                                    type: string
                                  type: array
                                excludeHtmlIds:
                                  description: List of HTML ids to exclude from the
                                    parsed content.
                                  items:
                                    type: string
                                  type: array
                                structuredContentTypes:
                                  description: 'Contains the required structure types
                                    to extract from the document. Supported values:
                                    shareholder-structure.'
                                  items:
                                    type: string
                                  type: array
                              type: object
                            ocrParsingConfig:
                              description: |-
                                Configurations applied to OCR parser. Currently it only applies to PDFs.
                                Structure is documented below.
                              properties:
                                useNativeText:
                                  description: If true, will use native text instead
                                    of OCR text on pages containing native text.
                                  type: boolean
                              type: object
                          type: object
                        type: array
                    type: object
                  id:
                    description: an identifier for the resource with format projects/{{project}}/locations/{{location}}/collections/default_collection/dataStores/{{data_store_id}}
                    type: string
                  industryVertical:
                    description: |-
                      The industry vertical that the data store registers.
                      Possible values are: GENERIC, MEDIA, HEALTHCARE_FHIR.
                    type: string
                  kmsKeyName:
                    description: |-
                      KMS key resource name which will be used to encrypt resources:
                      /{project}/locations/{location}/keyRings/{keyRing}/cryptoKeys/{keyId}
                      The KMS key to be used to protect this DataStore at creation time. Must be
                      set for requests that need to comply with CMEK Org Policy protections.
                      If this field is set and processed successfully, the DataStore will be
                      protected by the KMS key, as indicated in the cmek_config field.
                    type: string
                  location:
                    description: |-
                      The geographic location where the data store should reside. The value can
                      only be one of "global", "us" and "eu".
                    type: string
                  name:
                    description: |-
                      The unique full resource name of the data store. Values are of the format
                      projects/{project}/locations/{location}/collections/{collection_id}/dataStores/{data_store_id}.
                      This field must be a UTF-8 encoded string with a length limit of 1024
                      characters.
                    type: string
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  skipDefaultSchemaCreation:
                    description: |-
                      A boolean flag indicating whether to skip the default schema creation for
                      the data store. Only enable this flag if you are certain that the default
                      schema is incompatible with your use case.
                      If set to true, you must manually create a schema for the data store
                      before any documents can be ingested.
                      This flag cannot be specified if data_store.starting_schema is
                      specified.
                    type: boolean
                  solutionTypes:
                    description: |-
                      The solutions that the data store enrolls.
                      Each value may be one of: SOLUTION_TYPE_RECOMMENDATION, SOLUTION_TYPE_SEARCH, SOLUTION_TYPE_CHAT, SOLUTION_TYPE_GENERATIVE_CHAT.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
